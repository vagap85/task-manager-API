services:
  backend:
    build: .
    container_name: task-manager-backend
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - /app/__pycache__
    environment:
      - PYTHONUNBUFFERED=1
      - DEBUG=1
    restart: unless-stopped