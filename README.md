### 📋 Task Manager - Управление задачами
Полнофункциональное веб-приложение для управления задачами с бэкендом на FastAPI и фронтендом на React.

## 🎯 Содержание:

✅ Unit-тесты (8 тестов: 4 frontend + 4 backend)

✅ Миграции Alembic (папка alembic с файлами миграций)

✅ Dockerfile (готовые файлы в проекте)

✅ Авторизация (токен-аутентификация в коде)

## 📚 Скриншоты:<br>
API https://disk.yandex.ru/i/gGyYdbOup4ibPg<br>
Приложение https://disk.yandex.ru/i/dpkXgkkO494EkA<br>

# ✅ Alembic (миграции):
powershell
# Примените все миграции
alembic upgrade head

# Проверьте состояние
alembic current

# Проверьте историю
alembic history
# 📁 Финальная структура проекта:

task-manager/<br>
├── backend/<br>
│   ├── alembic/                    # ✅ Миграции<br>
│   │   ├── versions/<br>
│   │   │   ├── 001_initial_migration.py<br>
│   │   │   └── 002_add_priority.py<br>
│   │   ├── env.py<br>
│   │   ├── script.py.mako<br>
│   │   └── README.md<br>
│   ├── alembic.ini<br>
│   ├── Dockerfile                  # ✅ Докеризация<br>
│   ├── tests/<br>
│   │   ├── test_simple.py         # ✅ 4 unit-теста<br>
│   │   ├── test_auth.py           # ✅ Тесты авторизации<br>
│   │   └── test_tasks.py<br>
│   └── app/<br>
│       └── main.py                # ✅ С авторизацией<br>
├── frontend/<br>
│   ├── Dockerfile                 # ✅ Докеризация<br>
│   └── src/<br>
│       └── basic.test.js          # ✅ 4 unit-теста<br>
├── docker-compose.yml             # ✅ Docker Compose<br>
└── README.md                      # ✅ Полная документация<br>
## 🏗️ Технологический стек
# Backend (FastAPI)
Python 3.11+

FastAPI - современный веб-фреймворк

SQLAlchemy - ORM для работы с БД

SQLite - база данных (легкая, файловая)

Pydantic - валидация данных

Uvicorn - ASGI сервер

# Frontend (React)
React 18+

Material-UI (MUI) - компоненты интерфейса

Axios - HTTP клиент для API запросов

React Scripts - инструменты сборки

# 🛠️ Установка и запуск
1. Клонирование репозитория
bash
git clone https://github.com/vagap85/task-manager-API.git
cd task-manager
2. Настройка Backend
bash
cd backend

# Создание виртуального окружения (Windows)
python -m venv .venv
.venv\Scripts\activate

# Установка зависимостей
pip install -r requirements.txt

# Запуск сервера
uvicorn app.main:app --reload --port 8000
Backend будет доступен по адресу: http://localhost:8000

3. Настройка Frontend
bash
cd frontend

# Установка зависимостей
npm install

# Запуск React приложения
npm start
Frontend будет доступен по адресу: http://localhost:3000

## 📚 API Документация
После запуска бэкенда доступны:

Swagger UI: http://localhost:8000/docs

ReDoc: http://localhost:8000/redoc

OpenAPI JSON: http://localhost:8000/openapi.json

🔧 API Эндпоинты
Задачи (Tasks)
POST /tasks/ - Создание новой задачи

GET /tasks/ - Получение списка задач (с фильтрацией по статусу)

GET /tasks/{id} - Получение конкретной задачи по ID

PUT /tasks/{id} - Обновление задачи

DELETE /tasks/{id} - Удаление задачи

Примеры запросов
Создание задачи:

bash
curl -X POST "http://localhost:8000/tasks/" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Изучить FastAPI",
    "description": "Освоить основы FastAPI",
    "status": "in_progress"
  }'
Получение задач с фильтрацией:

bash
# Все задачи
curl "http://localhost:8000/tasks/"

# Только задачи в статусе 'pending'
curl "http://localhost:8000/tasks/?status=pending"
🧪 Тестирование
Backend тесты
bash
cd backend
pytest tests/test_simple.py -v
Frontend тесты
bash
cd frontend
npm test -- basic.test.js
🔍 Основные функции приложения
1. Создание задачи
Обязательное поле: заголовок (title)

Опциональное поле: описание (description)

Статус по умолчанию: "pending"

Автоматически проставляется дата создания

2. Фильтрация задач
По статусу: pending, in_progress, done

Пагинация: параметры skip и limit

3. Валидация данных
Проверка обязательных полей

Валидация статуса (только разрешенные значения)

Автоматическое преобразование дат

🐳 Docker ```docker-compose up --build```<br>

🤝 Вклад в проект
Форкните репозиторий

Создайте ветку для новой функции (git checkout -b feature/amazing-feature)

Зафиксируйте изменения (git commit -m 'Add amazing feature')

Запушьте в ветку (git push origin feature/amazing-feature)

Откройте Pull Request

📄 Лицензия
Этот проект распространяется под лицензией MIT. 

✨ Автор
Vagap85 - GitHub

🙏 Благодарности<br>
FastAPI команде за отличный фреймворк

React сообществу

Material-UI за прекрасные компоненты

⭐ Если этот проект был полезен, поставьте звезду на GitHub!
